# Vite proxy kullanılıyor — geliştirme ortamında bu dosyaya gerek yok.
# Proxy: vite.config.js -> server.proxy -> /api -> http://localhost:5000
# Production build için gerekirse VITE_API_URL'yi tanımlayın.
# VITE_API_URL=https://api.yourdomain.com
